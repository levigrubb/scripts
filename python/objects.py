#!/usr/bin/env python3

import subprocess

class MyClass:
    def __init__(self, cmd):
        cmd = self.cmd:w
        item = subprocess.getoutput(cmd)

    def __repr__(self):
        print(f'object: (name="{self.cmd}")')

    def function(
